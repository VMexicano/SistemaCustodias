// ---------------------------------------------------------------------------
// monitor-engine.types.ts — shared types for the Monitor Engine module (Sprint 15)
// ---------------------------------------------------------------------------

export interface MonitorJobData {
  eventId: string;
  orderId: string;
}
