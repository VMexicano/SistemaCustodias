Voy a preparar el contexto para trabajar en el módulo: $ARGUMENTS

Ejecuta estos pasos:

**Paso 1:** Lee context/router.md y determina el tipo de contexto para este módulo.

**Paso 2:** Carga exactamente los archivos indicados por el router para ese tipo. No más.

**Paso 3:** Lee context/snapshots/$ARGUMENTS.snapshot.md para entender el estado actual del módulo.

**Paso 4:** Lee los contratos relevantes en docs/09_api_contracts.md (solo los endpoints de este módulo).

**Paso 5:** Lee las tablas relevantes en docs/10_data_dictionary.md (solo las tablas de este módulo).

**Paso 6:** Muéstrame:
- Estado actual del módulo (del snapshot)
- Archivos que ya existen vs los que hay que crear
- Reglas de negocio críticas que aplican
- Specs de tests requeridos (del PLAN_TDD_SDD.md)
- Lista de tareas concretas para completar el módulo

**Paso 7:** Actualiza context/session.md con el módulo en foco y los archivos cargados.

Esperamos el módulo: $ARGUMENTS
